# Optional. Copy to `.env` and uncomment what you need.
# Nothing here is required for a first install — docker compose defaults work.

# LOPINGO_DB_PATH=/data/lopingo.db
# LOPINGO_TICK_SECONDS=5
# LOPINGO_MAX_PARALLEL=10
# LOPINGO_BATCH=100
# LOPINGO_CHECK_RETENTION_DAYS=30
